User is reporting that they cannot log back into their computer after being away for 3 weeks on a vacation.
It tells user the password is expired and it needs to be changed.
User cannot themselves change this.

I visit their account in the user directory and visit the authentication tab.
This page confirms indeed that the password has expired.

Instead of instantly reset a password, we can send a verification code to confirm the users identity.


The user gets a code sent and they need to send it back to me through the company chat so I can put it into the verification field.

After the user has gotten verified, we will then resend a temp password to the user.
I get confirmation that the user has managed to log in and change the password and everything is working.

Ticket closed.
